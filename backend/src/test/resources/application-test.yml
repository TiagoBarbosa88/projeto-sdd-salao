spring:
  datasource:
    url: jdbc:h2:mem:salao;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true

jwt:
  secret: test-secret-key-with-at-least-32-chars
  expiration-ms: 3600000
